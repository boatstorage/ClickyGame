import React from "react";
import "./Header.css";

const Header = () => (
	<header className="header">
		<h1>Clicky Game</h1>
		<h2>Click on all the images only once!</h2>
	</header>

	);

	export default Header;